<UserControl x:Class="MEATaste.Views.MapVoltage.VoltageMapPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d"
             d:DesignHeight="280" d:DesignWidth="280"
             IsVisibleChanged="VoltageMapPanel_IsVisibleChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScottPlot:WpfPlot x:Name="PlotControl" Grid.Row="0" Loaded="PlotControl_Loaded" />

        <StackPanel Grid.Row="1" Margin="2,2,2,2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Height="22" Margin="0,0,2,0"
                         Text="{Binding TimeText, UpdateSourceTrigger=PropertyChanged}"
                         KeyUp="TimeText_KeyUp" ToolTip="Time (s)"/>
                <Button Grid.Column="1" Content="{Binding RunStopLabel}" MinWidth="40" Height="22" Click="RunStop_Click"/>
                <CheckBox Grid.Column="2" Content="Loop" VerticalAlignment="Center" Margin="4,0,0,0" IsChecked="{Binding Loop}"/>
                <ComboBox Grid.Column="3" Width="56" Height="22" Margin="4,0,0,0"
                          SelectedIndex="1" SelectionChanged="Speed_SelectionChanged" ToolTip="Playback speed">
                    <ComboBoxItem Content="0.5x"/>
                    <ComboBoxItem Content="1x"/>
                    <ComboBoxItem Content="2x"/>
                    <ComboBoxItem Content="5x"/>
                    <ComboBoxItem Content="10x"/>
                </ComboBox>
                <Button Grid.Column="4" Content="+" Width="24" Height="22" Margin="4,0,0,0" Click="ZoomIn_Click" ToolTip="Zoom in"/>
                <Button Grid.Column="5" Content="−" Width="24" Height="22" Margin="2,0,0,0" Click="ZoomOut_Click" ToolTip="Zoom out"/>
                <Button Grid.Column="6" Content="Fit" MinWidth="32" Height="22" Margin="2,0,0,0" Click="Fit_Click" ToolTip="Fit array"/>
            </Grid>
            <Grid Margin="0,2,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <CheckBox Grid.Column="0" Content="Auto" VerticalAlignment="Center"
                          IsChecked="{Binding AutoScale}" Checked="AutoScale_Changed" Unchecked="AutoScale_Changed"/>
                <Label Grid.Column="1" Content="±mV" Padding="6,0,2,0" VerticalAlignment="Center"/>
                <TextBox Grid.Column="2" Height="22" Text="{Binding ScaleAmplitudeText, UpdateSourceTrigger=PropertyChanged}"
                         KeyUp="ScaleText_KeyUp"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
