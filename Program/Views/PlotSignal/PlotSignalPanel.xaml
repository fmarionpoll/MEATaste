<UserControl x:Class="MEATaste.Views.PlotSignal.PlotSignalPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <WpfPlot x:Name="PlotControl"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row ="1" 
                 Loaded="PlotControl_Loaded" AxesChanged="PlotControl_AxesChanged" />
        <CheckBox Grid.Row="0" Margin="0,0,0,0" VerticalAlignment="Center" Checked="CheckBox_Checked" >
            <TextBlock Text="Load data for selected electrode" />
        </CheckBox>
    </Grid>

</UserControl>
