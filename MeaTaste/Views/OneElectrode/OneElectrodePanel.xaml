<UserControl x:Class="MEATaste.Views.OneElectrode.OneElectrodePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="0,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <WpfPlot x:Name="RawSignal"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row ="1" />
        <WpfPlot x:Name="FilteredSignal" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" Grid.Row ="2"/>
        <CheckBox FlowDirection="RightToLeft" Grid.Row ="0"  HorizontalAlignment="Right" Margin="0,0,20,0" VerticalAlignment="Top" Checked="CheckBox_Checked" >
            <TextBlock Text="Load data for selected electrode"/>
        </CheckBox> 
    </Grid>


</UserControl>
